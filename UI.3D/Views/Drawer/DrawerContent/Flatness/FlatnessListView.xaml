<UserControl x:Class="UI._3D.Views.Drawer.DrawerContent.Flatness.FlatnessListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI._3D.Views.Drawer.DrawerContent.Flatness"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DataGrid ItemsSource="{Binding FlatnessViewModels}" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True"/>
            <DataGridTextColumn Header="Value" Binding="{Binding Value, StringFormat=N3}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
