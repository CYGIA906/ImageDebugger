<local:UserControlBase x:TypeArguments="viewModels:ImageProviderViewModel" x:Class="UI.Views.ImageProviderView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:local="clr-namespace:UI.Views"
                       xmlns:viewModels="clr-namespace:UI.ViewModels"
                       xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                       mc:Ignorable="d"
                       d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Button
            Height="50"
            Margin="5 5"
            Command="{Binding SelectImageDirCommand}"
            Content="Select Image Directory" />
        <materialDesign:Badged BadgeColorZoneMode="PrimaryDark" Margin="5" Badge="{Binding NumImages}">

            <Button Style="{StaticResource MaterialDesignRaisedLightButton}"
                    Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}},Path=DataContext.ExecuteCommand}" CommandParameter="{Binding Path=NextImage}">Next</Button>
        </materialDesign:Badged>

    </StackPanel>

</local:UserControlBase>